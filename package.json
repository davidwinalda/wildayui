{
  "name": "wilday_ui",
  "version": "1.0.0",
  "description": "Short description and motivation.",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "build": "NODE_ENV=production node esbuild.config.js",
    "build:watch": "NODE_ENV=development node esbuild.config.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "esbuild": "^0.24.0"
  }
}
